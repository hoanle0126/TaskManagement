/**
 * Centralized animation tokens for Framer Motion.
 * All components MUST import from here — no inline animation values (G14).
 * Will be fully populated in Phase 2 (Design System).
 */

export const MOTION = {
  /** Placeholder — populated in Phase 2 */
} as const;
